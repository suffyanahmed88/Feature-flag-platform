[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "featureflag-sdk"
version = "0.1.0"
description = "Python client for Feature Flag Platform"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27"]
[tool.pytest.ini_options]
testpaths = ["tests"]
